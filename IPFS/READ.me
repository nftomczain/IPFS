# 🌍 IPFS Infrastructure — NetNeroJes

**Posten infrastruktura** NetNeroJes:  
Lokalny system publikacji treści przez IPFS + IPNS + DNSLink.

---

## 📦 Struktura
- `site/` — główna zawartość strony (HTML/CSS/JS)
- `scripts/deploy_ipfs.sh` — automatyczny deploy do IPFS + IPNS
- `.github/workflows/ipfs_deploy.yml` — CI/CD pipeline
- `manifest.md` — filozofia projektu (manifest NetNeroJes)

---

## ⚡ Deploy lokalny
```bash
bash scripts/deploy_ipfs.sh
